export const metadata = { title: "Daily" };

export default function DailyPage() {
  return (
    <div className="page-shell compact">
      <p className="eyebrow">Daily</p>
      <h1>Daily challenge infrastructure comes after the core runtime.</h1>
      <p className="lede">Linebreak Daily is planned as the second production experiment to test streaks, sharing, and return behavior without requiring accounts.</p>
    </div>
  );
}
